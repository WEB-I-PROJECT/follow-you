const AnalyticByProfile = require('../models/AnalyticByProfile');
const User = require('../models/User');

class AnalyticByProfileController {

    index(req, res) {
        // 
    }

}

module.exports = AnalyticByProfileController
